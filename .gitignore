node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# MCP config pode conter tokens — não commitar
.mcp.json
!.mcp.json.example
.meta-token.tmp

# Codex config contém SUPABASE_ACCESS_TOKEN — nunca commitar
.codex/

# Pacotes de entrega do designer (referência local, não versionar)
/export/
